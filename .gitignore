node_modules/
dist/
coverage/
*.tsbuildinfo

# Environment — never commit
.env
.env.*
!.env.example

# Local upload storage (dev). Note: anchored to repo root so it does NOT
# ignore the source module at src/modules/uploads/.
/uploads/

# Prisma generated client
src/generated/

# Pid/logs
*.pid
*.log
